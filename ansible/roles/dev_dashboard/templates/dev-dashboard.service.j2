[Unit]
Description=Dev Dashboard
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User={{ ansible_user }}
WorkingDirectory=/home/{{ ansible_user }}/dev-dashboard
EnvironmentFile=/home/{{ ansible_user }}/dev-dashboard/.env
Environment=NODE_ENV=production
ExecStart=/usr/local/bin/bun run src/server.ts
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
