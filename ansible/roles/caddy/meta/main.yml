---
galaxy_info:
  role_name: caddy
  author: ikaros-iac
  description: Deploy Caddy (with Cloudflare DNS plugin) as a native systemd service
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - noble  # 24.04 LTS

dependencies: []
