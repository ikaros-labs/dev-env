---
# Phase A: Clean up Docker-based CoreDNS (idempotent, skipped on fresh installs)

- name: Check for old Docker-based CoreDNS
  ansible.builtin.stat:
    path: /opt/coredns/docker-compose.yml
  register: old_coredns_compose

- name: Stop Docker-based CoreDNS
  ansible.builtin.command:
    cmd: /usr/bin/docker compose down
    chdir: /opt/coredns
  become: true
  when: old_coredns_compose.stat.exists

- name: Remove old /opt/coredns directory
  ansible.builtin.file:
    path: /opt/coredns
    state: absent
  become: true

# Phase B: Install native CoreDNS

- name: Create coredns system user
  ansible.builtin.user:
    name: coredns
    system: true
    shell: /usr/sbin/nologin
    home: /nonexistent
    create_home: false
  become: true

- name: Create CoreDNS config directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - /etc/coredns/conf.d
    - /etc/coredns/zones
  become: true

- name: Download CoreDNS release tarball
  ansible.builtin.get_url:
    url: "https://github.com/coredns/coredns/releases/download/v{{ coredns_version }}/coredns_{{ coredns_version }}_linux_amd64.tgz"
    dest: "/tmp/coredns_{{ coredns_version }}.tgz"
    mode: "0644"
  become: true
  register: coredns_download

- name: Extract CoreDNS binary
  ansible.builtin.unarchive:
    src: "/tmp/coredns_{{ coredns_version }}.tgz"
    dest: /usr/local/bin/
    remote_src: true
    include:
      - coredns
    mode: "0755"
    owner: root
    group: root
  become: true
  when: coredns_download.changed
  notify: Restart coredns

- name: Deploy Corefile
  ansible.builtin.template:
    src: Corefile.j2
    dest: /etc/coredns/Corefile
    owner: root
    group: root
    mode: "0644"
  become: true
  notify: Restart coredns

- name: Deploy coredns systemd unit
  ansible.builtin.template:
    src: coredns.service.j2
    dest: /etc/systemd/system/coredns.service
    owner: root
    group: root
    mode: "0644"
  become: true
  notify:
    - Reload systemd daemon
    - Restart coredns

- name: Ensure coredns service is enabled and running
  ansible.builtin.systemd:
    name: coredns
    enabled: true
    state: started
    daemon_reload: false
  become: true
